//Defined below is a user class.

class User {
  name;
  constructor(name) {
    
  }

  printName() {

  }
}

/*Whenever a user is created, be sure to store the parameter 
name in the constructor to the defined property called name.

When an instance calls printName, 
it should print out the user's name.*/